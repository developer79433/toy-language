fun make_counter() {
    var count = 0;
    fun get_next_count() {
        count++;
        return count;
    }
    return get_next_count;
}
var my_counter = make_counter();
assert(my_counter() == 0);
assert(my_counter() == 1);
assert(my_counter() == 2);
