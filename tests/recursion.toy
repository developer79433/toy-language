fun fib(n) {
    assert_gte(n, 0);
    if (n == 0) {
        return 1;
    } elseif (n == 1) {
        return 1;
    }
    return fib(n - 2) + fib(n - 1);
}
print(fib(0));
print(fib(1));
print(fib(2));
print(fib(3));
print(fib(4));
print(fib(5));
